{
  "name": "@metal/daemon",
  "version": "0.1.0",
  "description": "Daemon for Metal - AI-powered context switching tool",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npx tsup src/index.ts --format cjs --dts --clean --minify --sourcemap",
    "dev": "npx tsup src/index.ts --format cjs --dts --clean --watch",
    "lint": "eslint src --ext .ts",
    "clean": "rimraf dist",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "chalk": "^5.3.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.24",
    "eslint": "^8.57.0",
    "rimraf": "^5.0.5"
  }
}
